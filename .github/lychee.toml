# .github/lychee.toml
# 死链检查配置:容忍临时网络抖动,排除 localhost 等

max_retries = 2
retry_wait_time = 5
timeout = 20

# 排除常见私有/本地链接
# 同时排除站内根相对路径(/...):它们由 Astro base 拼接,
# 在本地文件系统中无法直接解析,且首次部署前远程也访问不到。
exclude = [
  "^https?://localhost",
  "^https?://127\\.0\\.0\\.1",
  "^https?://0\\.0\\.0\\.0",
  "^/",
]
